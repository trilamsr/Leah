# Changelog

## v1.1 (Phase 4 — 2026-06-23)

Phase 4 ship criterion met. Voice frontier runtime landed (Whisper-quality STT chain, BPE detokenizer, full-duplex coordinator, turn instrumentation) — answer-engine streams over voice without HUD focus. Multi-device sync brought up over Bonjour with OTP pairing, mTLS transport (`sync.NewMTLSConfig` shared-key derive), CRDT peer coordinator, attempt-counter for backoff; iCloud sync pane shipped in Settings. Recommend pass-2 added signal dispatcher with voice announcer, SQLite engine, macOS mirror source, anti-list A/B pane. Camera + vision shipped OCR engine with consent store + Swift HUD router. Multi-agent A2A landed inbound MCP token-scoped server with Connections pane, plus Leah-to-Leah client/server over ed25519 with consent + budget gating. Continuous attestation verifier + scoped question pool integrated against MCP inbound. Plugin SDK shipped host + sandbox + quota meter; weather plugin landed as reference. Privacy budget runtime (`budget.NewRuntime`) is the shared ledger debited by A2A, recommend, and vision. Watchdog supervisor brought up with heartbeat + circuit breaker + leak detector. Three superseded sketches (`docs/engineer/specs/2026-06-10-{voice-frontier,learn-recommend-apply,mcp-a2a-publish}.md`) deleted — fully subsumed by `docs/superpowers/designs/2026-06-22-leah-phase4-design.md`.

## v3.3.0 (2026-06-23)

Phase 3 ship criterion met. TTS subsystem landed (ElevenLabs Flash v2.5 cloud primary + Apple Ava Premium local fallback, `tts.cloud.frame` + `tts.local` IPC fan-out, daemon-side privacy classifier). Wake-word adapter shipped with `wake-leah.mlmodel` bundled under `Resources/Models/`, VAD-gate + per-app suppression list ON by default. Push-to-talk landed on Fn (internal) and right-⌘ (external). Minimal-mode runtime toggle wired to Settings → Appearance — strips grain, italic, gold-accents at runtime. Touch ID gate added for memory purge + telemetry-toggle per §17.13. Push-source IPC fan-out completed (knowledge + memory + integrations push deltas to HUD). KG-backed citations join the answer-engine streaming path. MCP publish ships read-only (queries only, no mutations). Sparkle auto-appcast generator landed with EdDSA verify + rollback channel — appcast hosted on GitHub Pages, EdDSA signing-key custody per §17.19. §4.7 dashboard surface implemented (Memory + agenda + briefs + news + knowledge views over existing widget adapters). §17.12 marketing-hero asset slots finalized (4 hero PNGs + SVG/PDF mark).

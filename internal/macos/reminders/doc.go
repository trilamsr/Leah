// Package reminders reads tasks from Reminders.app's local Group Container
// SQLite store (read-only, attestation-gated) and exposes them via the
// MacApp interface per docs/engineer/specs/2026-06-10-macos-ecosystem-integration.md (W26).
package reminders

// Package higgsfield is Leah's adapter for the Higgsfield image + short-clip
// API. Every secret-touching RPC routes through the operator-attestation gate
// before the bearer key is loaded; audit rows hash prompts to keep plaintext
// out of disk. Used by the strategist composer to attach an image + clip to
// each draft post.
package higgsfield

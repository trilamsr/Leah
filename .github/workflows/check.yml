name: check

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  pull-requests: read

jobs:
  check:
    name: build + test + vet + lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'
          cache: true

      - name: Verify go.mod
        run: |
          go mod download
          go mod verify

      - name: Build
        run: go build ./...

      - name: Test
        run: go test ./... -race -count=1

      - name: Vet
        run: go vet ./...

      - name: Lint
        uses: golangci/golangci-lint-action@v7
        with:
          version: v2.12.2
          args: --timeout=5m

  phase2-smoke:
    name: phase2 e2e smoke (macOS)
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'
          cache: true

      - name: Phase 2 e2e
        if: runner.os == 'macOS'
        run: make phase2-smoke

  phase3-smoke:
    name: phase3 e2e smoke (macOS)
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'
          cache: true

      - name: Phase 3 smoke unit-test
        if: runner.os == 'macOS'
        run: bash scripts/dev/phase3-smoke_test.sh

      - name: Phase 3 e2e
        if: runner.os == 'macOS'
        env:
          LEAH_E2E_STUB: '1'
        run: bash scripts/dev/phase3-smoke.sh

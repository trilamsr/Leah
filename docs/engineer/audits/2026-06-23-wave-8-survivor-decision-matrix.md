---
title: Wave-8 survivor decision matrix
date: 2026-06-23
audience: operator
purpose: enable a Phase-4 ship/kill decision on the 11 Wave-8 impl waves that issue #249 flagged as "NOT YET shipped"
input: docs/engineer/briefs/2026-06-10-wave-8-aiml-upgrade.md, issue #249, residual-backlog memory 2026-06-23, polymath wedge research 2026-06-21
---

# Wave-8 survivor decision matrix (2026-06-23)

## Status

11 impl waves reviewed (W121, W122, W123, W125, W126, W127, W133, W134, W136, W142, W143 — the set issue #249 closed against). Cross-checked each against tree state, merged PR history, Phase-3/Phase-4 plan docs, and polymath wedge anti-signals.

- **2 already shipped via Phase 2/3** — mark complete (W142, W143-equivalent).
- **5 recommended survivors** (Phase 4 candidates) — W121, W122, W123, W125, W126.
- **4 recommended kills** — W127, W133, W134, W136.

Operator-decision input: the brief's own framing says S10 "operator-trust moat artifacts" each were "~1 spec + ~1 PR" net-news ships. The kill recommendations below are based on (a) zero pull-signal from the operator backlog, (b) lower-leverage than Phase-4 voice/vision/plugin work that supersedes them, and (c) the M4 (`LEAH_LOCAL_ONLY`) path is itself flag-gated on V10 Ollama reasoner integration which has not landed — so shipping the wiring without the reasoner ships a stub.

The 5 survivors are the **knowledge-graph + consolidation closeout** — they pick up where W120 (PR #192) and W124 (PR #219) left off and complete a single coherent surface that polymath wedge research P1 names as Leah's strongest defensible moat.

## Recommended survivors (Phase 4 candidates)

- **W121 — `Graph.QueryLive` + cap.** Adds the live-query type and cap to `internal/knowledge/graph.go` + `storage.go`. Spec: `docs/engineer/specs/2026-06-10-knowledge-graph-wiring.md` §3, §7. **Tree state:** `demotion` column exists on `entities` (storage.go:78), but no `QueryLive` type defined yet (`grep -rn "func.*QueryLive" internal/knowledge/` returns 0). **Why ship:** the demotion column is already paying for itself in `KnowledgeGraphSource` (PR #192 / W120 merged @ `7ce39bae`), but no caller can read ranked-live entities. Without W121 the W120 weight-filter is half-wired. **Dep:** none unmet — additive ALTER TABLE on a table that already shipped.
- **W122 — TTL + demotion logic.** New `internal/knowledge/ttl.go` + `ttl_test.go`, one-line hook into `graph.go` `Update`. **Tree state:** file absent (`ls internal/knowledge/ttl* → no match`). **Why ship:** the demotion column without the time-decay update path means weights only move on explicit `forget`, not staleness — the stated "Sarah is now ex-coworker" use case from S8 §3 stays broken. **Dep:** W121 (`QueryLive` reads `demotion` updated by `ttl.go`).
- **W123 — `OnSignal` consumes Graph + `leah forget --relation`.** Wires the dispatcher to query `Graph.QueryLive`, adds `--relation` flag to `cmd/leah/forget.go`. **Tree state:** `--relation` absent from `forget.go` (grep returns 0); `internal/recommend/` calls `knowledge.Graph.Query` only via sources/knowledge.go (recommend_test seam). The S8 "flip-day wave" never flipped. **Why ship:** without W123 the entire knowledge-graph wiring spec is shipped-but-inert at the operator-facing surface — no command exists to forget a relation, no Signal consumes a live graph query. This is the regressed-feature repair S8 was named for, still regressed. **Dep:** W121 + W122.
- **W125 — `ConsolidatePass` operational beyond schema.** `internal/operatormodel/consolidate.go` exists and has `ConsolidatePass` defined (consolidate.go:17). **Tree state:** PARTIAL — the W124 schema + the pass entry point shipped (PR #219, ecdf186c), but consolidation tests in `memory_test.go` directly upsert via raw SQL rather than calling the production path (memory_test.go:494 godoc explicitly notes this). **Why ship:** the stability gate + atomic-move + snapshot-upsert pipeline still has gaps — finishing W125 is the difference between "schema + skeleton" (current) and "consolidation actually runs nightly with operator-visible effect". **Dep:** none unmet.
- **W126 — dual-read Update + `consolidated_loader.go`.** **Tree state:** `loadConsolidatedSince` symbol absent (`grep -rn "loadConsolidatedSince" internal/ → 0`). Profile.go Update path does not read from `operator_profile_consolidated`. **Why ship:** without dual-read, the consolidated rows W125 writes are write-only — no caller reads them. Like W123, this is the closeout that makes the prior-wave work observable. **Dep:** W125.

## Recommended kills

- **W127 — daemon Daily-task wiring + `replay.go`.** **Tree state:** PARTIAL-SHIPPED-DIFFERENTLY. `cmd/leah/suggest.go` already has `suggest replay --since=<RFC3339>` (suggest.go:26-205), but the spec'd file `internal/operatormodel/replay.go` does not exist — replay was implemented inline in suggest.go instead. The daemon Daily task wiring is the only true gap. **Why drop:** the spec'd architecture (separate `replay.go` package) is already obsoleted by the inline implementation. Either (a) the inline shipping is sufficient — discharge W127 — or (b) the daemon Daily task is the residual ask, which is one cron-line in `cmd/leah-daemon/`. Either way, the ticket is mis-shaped. Cite of duplicate: `cmd/leah/suggest.go:26-205` ships the operator-facing replay verb; spec's `internal/operatormodel/replay.go` is an architecture-only refactor with no felt-UX delta.
- **W133 — `LEAH_LOCAL_ONLY=1` + egress instrumentation.** **Tree state:** `EgressBytes` field already exists in `reasoner/reasoner.go:25,38,167` and `audit/audit.go:35` (shipped via W92/W93 LLM-dim observability, PR #196); `LEAH_LOCAL_ONLY` is documented in trust-moats spec only, no impl exists in `internal/connect/instrumentation.go` (grep returns 0). **Why drop:** the spec itself (trust-moats.md:122) says M4 "lands behind a feature flag … until V10 lands" — V10 (Ollama reasoner) has not shipped. Shipping the wiring + audit verification without the reasoner ships an error-message-only flag that no operator can actually flip. Cite of anti-signal: residual-backlog 2026-06-23 OPERATOR-DECISION section names **zero** ask for `LEAH_LOCAL_ONLY` in the operator's "path-a-full personal-use" memory; this is a moat artifact, not a felt need.
- **W134 — `docs/operator/data-flow.svg`.** **Tree state:** file absent. **Why drop:** "an afternoon of work" (per S10 §10), zero code, no behavior change. If the operator ships a tap formula at all, the operator-facing audit story is README + `leah whoami` not an SVG. Cite of anti-signal: the brief itself says "Data-flow SVG: one diagram, every store, every network egress — afternoon of work" — this is documentation labor, not engineering, and unbounded-scope (every adapter, every egress) for a single-operator tool means it dates the day it ships. Phase-4 spec (lines 869-1493) treats the trust surface as continuous attestation, not a static SVG.
- **W136 — per-category memory attestation at first sight.** **Tree state:** `--category` absent from `cmd/leah/forget.go` (grep returns 0); no category attestation in `internal/memory/`. **Why drop:** S10 spec §11 itself flags M7 ships "dormant — operator sees no behavior change until tagging lands". Without the upstream category-tagging wave, this is wiring-with-no-trigger. Also anti-signal: polymath wedge research P4 ("local-first sovereign reasoner") is *demoted* per residual-backlog 2026-06-23 KILL section ("polymath as P1 wedge — labeled 'double down' but no shipped surface in 6 months"). M7 is the operator-facing surface of that wedge.

## Already shipped (mark complete in issue #249)

- **W142 — Sparkle EdDSA appcast generator + verify + rollback channel.** **SHIPPED via PR #353 (commit `9661c1f8`, merged 2026-06-22) + PR #421 (commit `9af00ffa`, merged 2026-06-23).** Pivot from spec: shipped as Swift (`app/Leah/Sources/LeahUpdate/Verify.swift`, `Updater.swift`, `AppcastTemplate.swift`) rather than Go (`internal/selfupgrade/verify_appcast.go` as the spec called for). Functionally complete — appcast generator (`scripts/release/generate-appcast.sh`), EdDSA verify on install, rollback channel in Settings → About. **Residual operator-action**: residual-backlog 2026-06-23 OPERATOR-DECISION section notes "EdDSA private key + appcast hosting are operator-side" — code is shipped, key custody + hosting URL are operator deliverables that no code agent can complete.
- **W143 — brew formula update automation.** **PARTIALLY SHIPPED via PR #263 (2026-06-21, "publish source tarball runbook for brew formula").** The tarball publish step the formula's `url` stanza references is wired into `.github/workflows/release.yml:75-76` (cites MAY-268). The spec'd `scripts/release/update-brew-formula.sh` that opens an auto-PR against `trilamsr/homebrew-leah` is the residual gap — but per the spec itself (signed-distribution.md:170), "Operator merges manually — the human gate is intentional." That makes W143's auto-PR script a courtesy, not a blocker, given the operator already gates merges. Recommend: discharge as shipped-enough; if missing the auto-PR ever bites, file a new ticket.

## Notes for the operator decision

1. **The 5 survivors form one coherent ship**: W121→W122→W123 closes the knowledge-graph wiring loop; W125→W126 closes the memory-consolidation loop. Both are the "regressed-feature repair" pattern S8's brief named — spec landed on main but the operator-facing surface stayed inert. The polymath wedge research (P1, P2) cites exactly this graph + consolidation surface as Leah's strongest defensible moat. Even with polymath demoted as a wedge label, the wiring closeout still discharges shipped-but-inert debt.
2. **The 4 kills are all S10/S12 trust-moat polish** with no pull signal — not part of the "path-a-full personal-use" memory, not on the Phase-4 plan critical path, and (for W133/W136) blocked on upstream waves that have not landed.
3. **Spec-vs-reality drift**: W127 was implemented in a different file than spec'd (inline in `cmd/leah/suggest.go` rather than `internal/operatormodel/replay.go`). W142 was implemented in a different language than spec'd (Swift, not Go). Issue #249 closing on these waves should reference the actual implementing PRs, not the spec'd architecture.
4. **No new spec PR needed**: surviving waves W121-W123 + W125-W126 are already spec'd in `2026-06-10-knowledge-graph-wiring.md` §7 (W121-W123) and `2026-06-10-memory-consolidation.md` §17 (W124-W127 table at line 289-292). Dispatch can begin against those existing specs once the operator confirms the survivor set.
